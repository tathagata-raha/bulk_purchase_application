import React, { Component } from 'react';
import axios from 'axios';

export default class EmptyCustomer extends Component {
    render() {
        return (
            <div>
            </div>
        )
    }
}
